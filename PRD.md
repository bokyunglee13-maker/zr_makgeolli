# PRD — ㅈㄹ막걸리 × DOOTA 팝업 웹사이트

> 최종 업데이트 기준: 현재 코드(index.html / name.html / survey.html / Code.gs) 상태

## 1. 개요
- **무엇**: 두타몰 B2F에서 열리는 'ㅈㄹ막걸리 × DOOTA' 팝업 행사용 다국어(한·영·일·중) 원페이지 웹사이트 + 부가 페이지.
- **누구**: 내국인 + 외국인 방문객. (설문은 외국인 대상)
- **톤**: 브루탈리스트 + 키치, 핑크 무드 (디자인 시스템은 `DESIGN.md`).
- **기간/장소**: 2026.6.8 – 6.30 · 두타몰 B2F (서울 중구 장충단로 275) · 운영 14:00–24:00 (MON–SUN) · 무료 입장.
- **슬로건**: `DRINK HAPPY NOT HEAVY`

## 2. 페이지 & 라우트 (Vercel)
| 페이지 | 파일 | 라우트 | 비고 |
|---|---|---|---|
| 랜딩 | `index.html` | `/`, `/ko` `/en` `/ja` `/zh` | 언어별 QR. 기본 ko |
| 초성 생성기(단독) | `name.html` | `/name` (+`?lang=`) | 생성기만 단독 노출 |
| 이름 궁합 테스트 | `match.html` | `/match` | **한국어 전용**(한글 획수 궁합). 랜딩에 진입 배너 |
| 시음·브랜드 설문 | `survey.html` | `/survey`, `/survey/ko` `/survey/en` `/survey/ja` `/survey/zh` | 외국인 대상, 기본 en, 언어별 QR |

- 언어 결정 우선순위: **URL 경로(/en) > `?lang=` > localStorage > 기본값**.
- 이미지/에셋은 `/assets/...` 루트 절대경로 (하위 경로 라우트에서도 안 깨지게).

## 3. 랜딩(index.html) 섹션 구조 (스크롤 순)
1. **플로팅 네비** — 로고(ㅈㄹ막걸리) · pill(ABOUT/TASTE/PROGRAM/VISIT) · 언어토글 · 인스타
2. **HERO** — 대형 타이틀 `ㅈㄹ막걸리 × DOOTA 팝업` (DOOTA 좌우 그라데이션) + 슬로건 `DRINK HAPPY NOT HEAVY` + 회전 스크롤 인디케이터
3. **마퀴** — `ㅈㄹㅁㄱㄹ` / `ZR MAKGEOLLI × DOOTA` 스큐 무한스크롤
4. **ABOUT** — 브랜드 소개
5. **MEANING** — 'ㅈㄹ'(영문판 WTF) 대형 글자 + 의미 단어가 떠다님 (재력·자랑·진리·잘래?·재롱·주류·정력·조롱·지랄·졸라 / WTF 확장어구)
6. **TASTE** — 맛 설명 + 스펙 배지(춘천쌀 100%·인공감미료 ZERO·ALC 7%·650ML)
7. **MADE WITH** — 제조 포인트 01~05 리스트 (당일도정·첨가물0%·50일저온숙성·참외멜론풍미·부드러운질감)
8. **GALLERY** — 6컷 슬라이드 카루셀 (자동회전+화살표+점)
9. **PROGRAM** — 체험 리스트 4종
   - 01 DOOTA 한정판 막걸리 (정보 표시, 클릭/이동 없음)
   - 02 시음 (클릭/이동 없음)
   - 03 막꾸 (클릭 토글 → 키링 사진 `makku.jpg` 카드 표시)
   - 04 Korean Alphabet Name (클릭 → 생성기로 이동)
10. **KOREAN NAME GENERATOR** — 띠배너 + 가운데 폼 → 결과를 **인스타 스토리 카드(1080×1920) 모달**로 (이미지 저장 / 공유)
10-1. **NAME MATCH 배너** — `/match`(이름 궁합 테스트)로 가는 다크 진입 배너 (4개 언어 노출, 페이지는 한국어 전용)
11. **VISIT** — 기간·장소·운영시간·입장 + 기프트 안내 배너
12. **APPLY (SNS 인증 기프트 뽑기)** — 아래 4번
13. **FOOTER** — 인스타 + 개인정보처리방침 (스토어 링크 없음)

## 4. 핵심 기능

### 4-1. Korean Alphabet Name 생성기
- 입력 이름 → 한글 초성 변환. 엔진은 **공유 파일 `/assets/chosung.js`**(`toChosung(name, lang)`), index/name 공통 로드. **언어별 규칙 분기**: ko/en=라틴 휴리스틱(모음 이중자·받침 흡수, en은 묵음 final-e), ja=로마자 모라(어두 무성음 ㄱ/ㄷ, 어중 ㅋ/ㅌ, f→ㅎ), zh=병음 분해(복운모 ao/ei→ㅇ 추가). **예외 테이블(NAME_MAP)**: 한국 성씨·영·일·중 불규칙 이름의 최종 폴백. (한글 입력은 언어 무관 직접 처리.)
- 변환 예시(설명부, 3줄 줄바꿈, **결과 간격 통일**): `JANG WON YOUNG → ㅈ ㅇ ㅇ`, `JUNGKOOK → ㅈ ㄱ`, `Rihanna → ㄹ ㅎ ㄴ`. (예시 결과는 가독성 위해 글자마다 띄어쓰기로 통일; 실제 생성 결과는 한 단어는 붙고 `김민지 → ㄱㅁㅈ`식, 띄어쓴 이름은 단어 사이가 벌어짐) 그 외 `장원영 → ㅈㅇㅇ`, `Tomiyasu → ㅌㅁㅇㅅ`, `Wang Wei → ㅇ ㅇㅇ` 등.
- 입력칸 placeholder: 언어별 **흔한 이름**으로 "본인 이름 입력" 유도 — KO `김민지` · EN `EMMA` · JA `HIMARI` · ZH `XINYI` (일/중은 "로마자/병음 입력" 안내 유지).
- 결과는 **스토리 카드(1080×1920) Canvas** 렌더 → 보틀 누끼 사진 포함 → **[이미지 저장]**(다운로드) / **[인스타 공유]**(Web Share API; 모바일 공유시트 → 인스타 선택, 자동 게시는 불가).
- 단독 페이지 `/name` 로도 제공(언어별 QR 가능).

### 4-2. SNS 인증 기프트 뽑기 (가챠) — APPLY 섹션
- 플로우: ① @zr.makgeolli 팔로우 → ② 팝업스토어/막걸리+키링 사진 스토리 + @멘션 → ③ 인스타 아이디 입력 + 인증 체크 → **랜덤 기프트 뽑기**.
- **확률**: 🔑 막꾸 키링 **10%** · 🪞 거울 굿즈 **40%** · 🏷️ 현장 할인 쿠폰 **50%**.
- 결과 모달: 두근두근 연출 → 당첨 기프트 + **고유 코드**(키링 `ZR-K-####`/거울 `ZR-M-####`/쿠폰 `ZR-D-####`). 할인쿠폰은 `COUPON` 배지로 표시.
- 결과 버튼: **이미지 저장(메인, 흰 버튼)** + 공유·팔로우·닫기(투명 아이콘). 저장 시 **브랜드 기프트 티켓 이미지(1080×1350, 캔버스)** — 코드 + "코드·스토리 보여주세요" 안내 포함. 공유=Web Share(바이럴).
- 안내문: **"현장 스태프에게 ① 이 코드와 ② 인스타 스토리를 함께 보여주세요"**(4개 언어).
- **검증**: 인스타 팔로우/멘션은 웹 자동검증 불가 → **현장 스태프 육안 확인 후 지급**(코드 이미지=교환권, 스토리=라이브 본인 인증). **인스타 아이디 미저장**(현장 확인용 입력만, 시트엔 빈값). **개인정보 동의 체크박스 없음**.
- 데이터 → 구글시트 `gifts` 시트.
- **중국어(zh) 변형**: 중국은 Instagram 불가 → zh 버전만 **小红书(RED)** 흐름. **계정: 小红书号 `Tipsyrice` / 닉네임 `Makgeolli lover`.** 팔로우=关注(在小红书搜索「Tipsyrice」), 스토리 대신 **笔记(피드 포스트)** 발행 + 멘션 `@Makgeolli lover` + 태그 `#韩国旅游 #韩国旅行 #韩国米酒 #首尔美食 #首尔必吃` + **DOOTA 地点(위치)**. 팔로우 버튼 + **푸터 SNS**(`footer-insta`, zh는 "小红书 Tipsyrice")는 `lang==='zh'`일 때 `CONFIG.XHS_URL = search_result?keyword=Tipsyrice`(검색 딥링크; 小红书 웹→앱 불안정해 ID 검색 유도). 다른 언어는 Instagram 유지. **검색/팔로우/푸터에 닉네임 대신 유일 ID(`Tipsyrice`)를 쓰는 이유**: 小红书 닉네임은 중복 가능(동명이인·사칭)이라 닉네임 검색은 엉뚱한 계정이 뜰 수 있음. @멘션만 닉네임 불가피.

### 4-4. 이름 궁합 테스트 (match.html, 한국어 전용)
- 커플/친구 **두 사람 한글 이름** 입력 → **한글 획수 궁합** 점수. 알고리즘: 각 글자를 초/중/종성 분해 후 자소 획수 합산 → 두 이름 획수를 교차 배열 → 인접 합 mod 10 반복 → 최종 2자리(점수). 전통 "이름 궁합" 방식.
- **양방향**: 이름 순서를 바꿔 계산(`getScore(A,B)` / `getScore(B,A)`)해 "각자 기준 점수"를 보여줌(순서 의존). 헤드라인 = 두 점수 평균. 높은 쪽이 "더 많이 좋아하는" 사람.
- **키치 연출**: 글자별 획수 카운트업(촤르륵) → 게이지 채움 + 점수 카운트업 → 점수대별 이모지·막걸리 테마 멘트(천생연분~상극의 짜릿함 6단계)·태그.
- **인스타 스토리 카드(1080×1920 Canvas)**: `A ♥ B` + 점수% + 이모지 + 멘트 + 양방향 + 슬로건/핸들. 저장/공유(Web Share). 랜딩에 진입 배너(`match.h/d/cta`, 4개 언어 노출).
- ⚠️ 획수 궁합은 **한글 전용**(외국 이름은 획수 산출 불가) → KO UI·한글 입력만. 데이터 → `namegen` 시트(event=`match_*`).

### 4-3. 시음·브랜드 설문 (survey.html, 외국인 대상)
- 문항: ① 기본(국적 입력·성별) ② 전체적인 맛 만족도(필수, 1 별로~5 아주좋아요) ③ 디자인 호감도(1~5)·가격 적정성(1 비싸다~5 저렴하다) ④ 재구매 의향(1~5) ⑤ 자유 의견.
- 4개 언어, 기본 en. 데이터 → 구글시트 `survey` 시트.

## 5. 데이터 모델 (구글시트, Apps Script)
- `gifts`: timestamp · instagram · gift · followed · story · code · language
- `survey`: timestamp · language · nationality · gender · overall · design · price · repurchase · comment
- `namegen`: timestamp · event · name · chosung · language · page — 초성 생성기(event=generate/share/save, page=index/name) **+ 이름 궁합**(event=`match_generate/share/save`, page=`match`, name=`A × B`, chosung=점수) 공용 적재.
- `responses` (레거시, 현재 프론트 미사용): 과거 응모폼·쿠폰용. 코드엔 남아있으나 사이트에서 호출 안 함.
- **분석 2단**: ①구글시트 `namegen`(원본 이름+횟수) ②Vercel Web Analytics 커스텀 이벤트 `name_generate/share/save`(이름 없이 횟수·언어·페이지). 생성기 하단 "통계 수집" 안내문 표기(4개 언어).
- 한 개의 웹앱이 `type` 으로 분기: `gift` / `survey` / `namegen` / (default=레거시 응모).
- **`Dashboard` 시트**: Apps Script `buildDashboard()` (시트 메뉴 [📊 ZR > 대시보드 갱신])가 위 시트들을 요약 — 생성수·인기 이름 TOP10·공유/저장 전환율·설문 평균(맛/디자인/가격/재구매)·국적/성별 분포·기프트 실제 비율.

## 6. 전환(Conversion) 목표
- 1차: SNS 팔로우 + 스토리 인증(UGC 확산) → 기프트.
- 2차: 초성 생성기 카드 공유(바이럴).
- 2-1차: **이름 궁합 카드 공유**(커플/친구 단위 바이럴 — 캡처해 인스타 스토리 업로드 유도).
- 3차(외국인): 설문 데이터 수집(맛·디자인·가격·재구매).

## 7. 확정된 결정사항
- 마지막 "더 보러 가기" CTA 및 모든 **스토어 링크 삭제**.
- 응모폼(이름/연락처/경로/쿠폰) → **SNS 인증 기프트 뽑기로 대체**.
- 설문은 **외국인 대상 다국어 + 언어별 URL**.
- 도메인: Vercel 기본(`zr-makgeolli.vercel.app` 가정).

## 8. 미결정 / TODO
- [x] **WEBAPP_URL** 연결 완료 — Apps Script 웹앱 배포 + index/survey/name 적용 + 시트 적재(gift/survey/namegen) 검증 완료. Vercel Analytics도 Enable.
- [ ] **기프트 재고 캡** 적용 여부 — 한정수량 있으면 서버 추첨+재고 차감으로 전환(각 수량 필요).
- [x] **개인정보 동의** — (B) 채택: 인스타 아이디 미저장 + 동의 체크박스 제거 완료.
- [ ] **OG 도메인** 실제 배포 도메인으로 확정 (현재 하드코딩).
- [x] **小红书 계정 확정** — 小红书号 `Tipsyrice`/닉네임 `Makgeolli lover`, zh 전반 반영. 링크는 검색 딥링크(`keyword=Tipsyrice`) 채택 — 小红书 웹→앱 링크가 불안정해 프로필 URL 대신 ID 검색 유도가 더 확실(의도된 선택).
- [ ] 운영시간/개인정보 보유기간 문구 사업자 최종 확인.
